<%= form_for(unit_subtitle) do |f| %>
  <%= error_messages_for unit_subtitle %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
